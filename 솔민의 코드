input_string = input("입력: ")

if "출력" in input_string:
    if "더하기" in input_string:
        parts = input_string.split("더하기")
        numbers = []

        for part in parts:
            num = part.strip()
            if num.isdigit():
                numbers.append(int(num))
            else:
                try:
                    number = int(num.split()[0])
                    numbers.append(number)
                except (ValueError, IndexError):
                    continue  

        if numbers:
            result = sum(numbers)
            print(result)
        else:
            print("유효한 숫자를 입력하세요...")
    else:
        print("더하기 연산이 포함되어 있지 않아요..ㅠ")
else:
    print("출력이라는 단어가 포함되어 있지 않아요..ㅠ")

입력: 5 더하기 6 더하기 7 출력해바
출력: 18

*아직 더하기만 됨
